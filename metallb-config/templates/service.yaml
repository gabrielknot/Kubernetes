apiVersion: v1
kind: Service
metadata:
  name: {{ include "metallb-config.fullname" . }}
  labels:
    {{- include "metallb-config.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metallb-config.selectorLabels" . | nindent 4 }}
